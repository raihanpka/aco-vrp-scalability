"""
Shared test fixtures for the aco_vrp test suite.

Provides reusable pytest fixtures including:
    sample_instance    A minimal CVRPInstance with 5 customers for unit testing.
    sample_configs     A set of ACOConfig presets used in the scalability experiment.
    rc101_instance     The full Solomon RC101 instance (requires data download).
"""

# Fixtures will be defined here as the implementation progresses.
# Example:
# @pytest.fixture
# def sample_instance() -> CVRPInstance:
#     ...
