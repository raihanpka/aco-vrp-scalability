"""
Unit tests for the ACO core algorithm module (aco_vrp.core).

Tests cover:
    ACOConfig validation and defaults
    Ant solution construction
    Pheromone matrix initialization and update
    Evaporation and deposition correctness
    2-opt local search (if enabled)
    Convergence behavior on small instances
"""

# Tests will be implemented alongside the core module.
